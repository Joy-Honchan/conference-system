import ScheduleData from 'data/scheduleData.json'

interface ScheduleDataType {
    timeStamp: number
    meeting: {
        roomName: string
        startTime: string
        endTime: string
    }[]
}

function mySchedule() {

}